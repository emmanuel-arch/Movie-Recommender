/** Promo premium monthly charge (KES). Server must enforce — do not trust client-only amounts. */
export const PREMIUM_MONTHLY_KES_PROMO = 99;
/** Display-only list price crossed out on marketing pages. */
export const PREMIUM_MONTHLY_KES_LIST = 499;
